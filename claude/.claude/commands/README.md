# Custom Slash Commands

Place your custom slash commands here as `.md` files.

Example: Create `fix-types.md` with:
```markdown
Find all TypeScript type errors in the codebase and fix them.
```

Then use it with: `/fix-types`
